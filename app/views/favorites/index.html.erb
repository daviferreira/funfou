<h2>Favoritos</h2>

<% unless @favorites.empty? %>
  <% @favorites.each do |question| %>
  	<div class="user-questions">
  		<%= render question %>
  	</div>
  <% end %>

  <%= will_paginate %>
<% end %>