<%= form_for current_user.favorites.find_by_question_id(@question),
		:html => { :method => :delete },
		:remote => true	do |f| %>
	<div class="actions"><%= f.submit "", :class => "favorite", :title => "Remover dos favoritos" %></div>
<% end %>

