<% unless current_user.id == @question.user_id %>
	<div id="favorite-form">
		<% if current_user.favorite?(@question) %>
			<%= render 'unfavorite' %>
		<% else %>
			<%= render 'favorite' %>
		<% end %>
	</div>
<% end %>
